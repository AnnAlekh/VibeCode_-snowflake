const PROMPTS = {
  generateTask: (level, topic, language) => `
Создай алгоритмическую задачу уровня ${level} (Junior, Middle или Senior) по теме ${topic} на языке программирования ${language}.

Очень важно: 
- **все формулировки задачи, требования, подсказки, описания тестов и примеры должны быть на русском языке**;
- код и названия функций могут быть на английском, но текст — только по‑русски.

JSON структура:
{
  "task": "Краткое описание задачи",
  "requirements": ["требование 1", "требование 2"],
  "example": "Пример (на русском): вход → выход",
  "hint": "Краткая подсказка на русском",
  "starterCode": "def solution(arr):\\n    pass",
  "visibleTestCases": [
    { "input": "пример входных данных", "output": "ожидаемый вывод" }
  ],
  "hiddenTestCases": [
    { "input": "граничный случай", "output": "ожидаемый вывод", "description": "Граничный случай", "type": "boundary" },
    { "input": "большой вход", "output": "ожидаемый вывод", "description": "Тест производительности на больших данных", "type": "performance" }
  ]
}

Сгенерируй тесты так, чтобы они строго соответствовали условию задачи и покрывали типичные, граничные и сложные случаи. 
Уровень: ${level}, тема: ${topic}. 
Верни ТОЛЬКО JSON без markdown, без комментариев и поясняющего текста до или после объекта.
  `,

  generateNextTaskLegacy: (level, topic, language, previousTask, candidatePerformance) => `
Кандидат только что решил задачу уровня ${previousTask?.level || level}:
${previousTask?.description || previousTask?.task || 'описание недоступно'}

Его результаты и анализ:
${candidatePerformance ? JSON.stringify(candidatePerformance, null, 2) : 'нет данных'}

На основе этого подбери СЛЕДУЮЩУЮ задачу уровня ${level} по теме ${topic} на языке программирования ${language}.
Новая задача должна:
- соответствовать уровню сложности ${level} (Junior/Middle/Senior),
- логично продолжать предыдущую (усложнение, новые ограничения, граничные случаи),
- акцентировать проверку на аспектах, которые выглядят более слабыми по анализу performance.

Очень важно:
- **все формулировки задачи, требования, подсказки, описания тестов и примеры должны быть на русском языке**;
- код и названия функций могут быть на английском, но текст — только по‑русски.

JSON:
{
  "task": "Краткое описание",
  "requirements": ["требование 1", "требование 2"],
  "example": "Пример (на русском): вход → выход",
  "hint": "Подсказка (на русском)",
  "starterCode": "def solution(arr):\\n    pass",
  "visibleTestCases": [
    { "input": "пример входных данных", "output": "ожидаемый вывод" }
  ],
  "hiddenTestCases": [
    { "input": "граничный случай", "output": "ожидаемый вывод", "description": "Граничный случай", "type": "boundary" },
    { "input": "большой вход", "output": "ожидаемый вывод", "description": "Тест производительности на больших данных", "type": "performance" }
  ]
}

Уровень: ${level}. 
Верни ТОЛЬКО JSON без markdown, комментариев и дополнительного текста.
  `,

  analyzeSolution: (code, task, testResults, attempts) => `
Проанализируй это решение кандидата:

Задача: ${task.description}
Ожидаемая сложность: ${task.expectedComplexity || 'не указана'}

Код кандидата:
\`\`\`python
${code}
\`\`\`

Результаты тестов:
- Видимые тесты: ${testResults.visiblePassed ? '✅ ПРОШЛИ' : '❌ НЕ ПРОШЛИ'}
- Скрытые тесты: ${testResults.hiddenPassed ? '✅ ПРОШЛИ' : '❌ НЕ ПРОШЛИ'}
- Попыток: ${attempts}

Оцени:
1. Правильность (0-100): Насколько правильное решение? Проходят ли все тесты?
2. Оптимальность (0-100): Соответствует ли временная и пространственная сложность ожидаемой?
3. Качество кода (0-100): Читаемость, стиль, структура, именование переменных
4. Временная сложность: Big O notation (например, O(n), O(n²))
5. Пространственная сложность: Big O notation (например, O(1), O(n))
6. Фидбек: Конструктивная обратная связь
7. Сильные стороны: Массив сильных сторон решения
8. Слабые стороны: Массив областей для улучшения
9. Предложения: Конкретные предложения по улучшению

Верни ТОЛЬКО валидный JSON:
{
  "correctness": 85,
  "optimality": 70,
  "codeQuality": 80,
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(1)",
  "feedback": "Хорошее решение, но можно оптимизировать...",
  "strengths": ["Четкая логика", "Хорошие имена переменных"],
  "weaknesses": ["Можно улучшить читаемость", "Не оптимальная сложность"],
  "suggestions": ["Использовать более эффективный алгоритм", "Добавить комментарии"]
}
  `,

  analyzeError: (code, task, failedTests, visiblePassed) => `
Решение кандидата прошло видимые тесты, но не прошло скрытые тесты.

Задача: ${task.description}

Код кандидата:
\`\`\`python
${code}
\`\`\`

Провалившиеся тесты:
${JSON.stringify(failedTests, null, 2)}

Видимые тесты: ${visiblePassed ? '✅ прошли' : '❌ не прошли'}

Объясни:
1. Почему видимые тесты прошли, а скрытые нет?
2. Какой тип ошибки это (граничный случай, edge case, производительность, логическая ошибка)?
3. Как исправить (предоставь руководство, но не полное решение)?

Будь полезным и образовательным. Направь кандидата к правильному решению.
  `,

  generateFollowUpQuestion: (task, solution, testResults, analysis) => `
Кандидат решил первую задачу: ${task.description || task.task || 'задача'}

Детали задачи:
${task.requirements ? `Требования: ${Array.isArray(task.requirements) ? task.requirements.join(', ') : task.requirements}` : ''}
${task.constraints ? `Ограничения: ${Array.isArray(task.constraints) ? task.constraints.join(', ') : task.constraints}` : ''}
${task.expectedComplexity ? `Ожидаемая сложность: ${task.expectedComplexity}` : ''}
${task.examples && task.examples.length > 0 ? `Примеры: ${task.examples.map(e => `${e.input || ''} → ${e.output || ''}`).join('; ')}` : ''}
${task.example ? `Пример: ${task.example}` : ''}

Его решение:
\`\`\`python
${solution}
\`\`\`

Результаты анализа:
${analysis ? `- Правильность: ${analysis.correctness || 0}/100
- Оптимальность: ${analysis.optimality || 0}/100
- Временная сложность: ${analysis.timeComplexity || 'не определена'}
- Пространственная сложность: ${analysis.spaceComplexity || 'не определена'}
${analysis.strengths && analysis.strengths.length > 0 ? `- Сильные стороны: ${analysis.strengths.join(', ')}` : ''}
${analysis.weaknesses && analysis.weaknesses.length > 0 ? `- Слабые стороны: ${analysis.weaknesses.join(', ')}` : ''}` : ''}

ВАЖНО: Сформулируй КОНКРЕТНЫЙ ТЕХНИЧЕСКИЙ вопрос от первого лица, который:
1. Напрямую связан с конкретными деталями решения (например, "Почему вы использовали именно эту структуру данных?", "Как ваш алгоритм обрабатывает случай, когда...", "Почему вы выбрали именно этот подход вместо...")
2. Проверяет понимание конкретных аспектов решения (выбор структур данных, обработка граничных случаев, оптимизация конкретных операций)
3. Ссылается на конкретные части кода или логику решения (например, "В вашем решении вы используете цикл внутри цикла - какова сложность этого фрагмента?", "Я вижу, что вы используете словарь - почему именно словарь, а не список?")
4. Учитывает специфику задачи (ограничения, требования, ожидаемую сложность)

НЕ задавай общие вопросы вроде "Можете объяснить решение?" или "Какова сложность?". Вместо этого задай конкретный вопрос про конкретный аспект решения, который виден в коде.

Формат: 1–2 предложения на русском языке, заканчивающиеся вопросительным знаком. Вопрос должен быть максимально конкретным и привязанным к деталям решения.
  `,

  generateTechnicalFollowUpQuestion: (task, solution, testResults, analysis) => `
Кандидат решил вторую задачу: ${task.description || task.task || 'задача'}

Детали задачи:
${task.requirements ? `Требования: ${Array.isArray(task.requirements) ? task.requirements.join(', ') : task.requirements}` : ''}
${task.constraints ? `Ограничения: ${Array.isArray(task.constraints) ? task.constraints.join(', ') : task.constraints}` : ''}
${task.expectedComplexity ? `Ожидаемая сложность: ${task.expectedComplexity}` : ''}
${task.examples && task.examples.length > 0 ? `Примеры: ${task.examples.map(e => `${e.input || ''} → ${e.output || ''}`).join('; ')}` : ''}
${task.example ? `Пример: ${task.example}` : ''}

Его решение:
\`\`\`python
${solution}
\`\`\`

Результаты анализа:
- Правильность: ${analysis?.correctness || 0}/100
- Оптимальность: ${analysis?.optimality || 0}/100
- Временная сложность: ${analysis?.timeComplexity || 'не определена'}
- Пространственная сложность: ${analysis?.spaceComplexity || 'не определена'}
${analysis?.strengths && analysis.strengths.length > 0 ? `- Сильные стороны: ${analysis.strengths.join(', ')}` : ''}
${analysis?.weaknesses && analysis.weaknesses.length > 0 ? `- Слабые стороны: ${analysis.weaknesses.join(', ')}` : ''}

ВАЖНО: Сформулируй КОНКРЕТНЫЙ ТЕХНИЧЕСКИЙ вопрос от первого лица, который:
1. Напрямую связан с конкретными деталями решения (например, "Почему вы использовали именно эту структуру данных?", "Как ваш алгоритм обрабатывает случай, когда...", "Почему вы выбрали именно этот подход вместо...")
2. Проверяет понимание конкретных аспектов решения (выбор структур данных, обработка граничных случаев, оптимизация конкретных операций)
3. Ссылается на конкретные части кода или логику решения (например, "В вашем решении вы используете цикл внутри цикла - какова сложность этого фрагмента?", "Я вижу, что вы используете словарь - почему именно словарь, а не список?")
4. Учитывает специфику задачи (ограничения, требования, ожидаемую сложность)

НЕ задавай общие вопросы вроде "Можете объяснить решение?" или "Какова сложность?". Вместо этого задай конкретный вопрос про конкретный аспект решения, который виден в коде.

Формат: 1–2 предложения на русском языке, заканчивающиеся вопросительным знаком. Вопрос должен быть максимально конкретным и привязанным к деталям решения.
  `,

  generateThirdQuestion: (task, solution, previousAnswer, analysis) => `
Кандидат решил вторую задачу: ${task.description || task.task || 'задача'}

Его решение:
\`\`\`python
${solution}
\`\`\`

Результаты анализа:
- Правильность: ${analysis?.correctness || 0}/100
- Оптимальность: ${analysis?.optimality || 0}/100
- Временная сложность: ${analysis?.timeComplexity || 'не определена'}
- Пространственная сложность: ${analysis?.spaceComplexity || 'не определена'}

Предыдущий технический вопрос и ответ кандидата:
${previousAnswer || 'Ответ недоступен'}

Сформулируй третий завершающий вопрос от первого лица, который углубляется в понимание решения или проверяет способность кандидата рассуждать о масштабируемости, альтернативных подходах, улучшениях или практическом применении. Вопрос должен быть более глубоким, чем предыдущий, и может касаться архитектурных решений, trade-offs, или реальных сценариев использования.

Формат: 1–2 предложения на русском языке, заканчивающиеся вопросительным знаком. Вопрос должен быть завершающим и показывать финальную оценку понимания.
  `,

  evaluateAnswer: (question, answer, solution) => `
Оцени ответ кандидата на твой вопрос.

Вопрос: ${question}
Ответ кандидата: ${answer}
Их решение: ${solution}

Оцени:
1. Score (0-100): Общее качество ответа
2. Understanding (0-100): Насколько хорошо кандидат понимает свое решение
3. Communication (0-100): Ясность объяснения
4. Feedback: Конструктивная обратная связь
5. Details: Детали оценки
6. IsSufficient (true/false): Достаточен ли ответ для определения уровня кандидата? Ответ считается недостаточным, если understanding < 70 или communication < 70 или score < 70

Верни ТОЛЬКО валидный JSON:
{
  "score": 85,
  "understanding": 90,
  "communication": 80,
  "feedback": "Хорошее понимание решения, но можно улучшить объяснение...",
  "isSufficient": true,
  "details": {
    "strengths": ["Понимает алгоритм", "Может объяснить сложность"],
    "weaknesses": ["Не упомянул альтернативные решения"]
  }
}
  `,

  generateAdditionalTechnicalQuestion: (task, solution, previousQuestion, previousAnswer, analysis, questionNumber) => `
Кандидат решил ${questionNumber === 1 ? 'первую' : 'вторую'} задачу: ${task.description || task.task || 'задача'}

Его решение:
\`\`\`python
${solution}
\`\`\`

Результаты анализа:
- Правильность: ${analysis?.correctness || 0}/100
- Оптимальность: ${analysis?.optimality || 0}/100
- Временная сложность: ${analysis?.timeComplexity || 'не определена'}
- Пространственная сложность: ${analysis?.spaceComplexity || 'не определена'}

Предыдущий вопрос: ${previousQuestion}
Ответ кандидата: ${previousAnswer}

Ответ кандидата был недостаточно подробным или не показал полного понимания решения. Сформулируй ДОПОЛНИТЕЛЬНЫЙ КОНКРЕТНЫЙ ТЕХНИЧЕСКИЙ вопрос от первого лица, который:
1. Проверяет другой аспект решения (не тот, что был в предыдущем вопросе)
2. Более конкретно связан с кодом (например, про конкретную строку, функцию, структуру данных)
3. Требует технического объяснения конкретной части решения
4. Помогает лучше понять уровень понимания кандидата

Вопрос должен быть про другой технический аспект, чем предыдущий. Например, если предыдущий был про сложность, этот может быть про выбор структуры данных или обработку граничных случаев.

Формат: 1–2 предложения на русском языке, заканчивающиеся вопросительным знаком. Вопрос должен быть максимально конкретным и привязанным к деталям решения.
  `,

  summarizeTaskConversation: (task, solution, analysis, chatHistory) => `
Резюмируй весь разговор по первой задаче для генерации второй задачи.

Задача: ${task.description || task.task || 'задача'}
Требования: ${task.requirements ? (Array.isArray(task.requirements) ? task.requirements.join(', ') : task.requirements) : 'не указаны'}
Ограничения: ${task.constraints ? (Array.isArray(task.constraints) ? task.constraints.join(', ') : task.constraints) : 'не указаны'}

Решение кандидата:
\`\`\`python
${solution}
\`\`\`

Анализ решения:
- Правильность: ${analysis?.correctness || 0}/100
- Оптимальность: ${analysis?.optimality || 0}/100
- Временная сложность: ${analysis?.timeComplexity || 'не определена'}
- Пространственная сложность: ${analysis?.spaceComplexity || 'не определена'}
- Сильные стороны: ${analysis?.strengths ? analysis.strengths.join(', ') : 'не указаны'}
- Слабые стороны: ${analysis?.weaknesses ? analysis.weaknesses.join(', ') : 'не указаны'}

История разговора (все вопросы и ответы):
${chatHistory.map((msg, i) => `${i + 1}. ${msg.role === 'assistant' ? 'Вопрос' : 'Ответ'}: ${msg.content}`).join('\n')}

Создай краткое резюме (2-3 абзаца) на русском языке, которое включает:
1. Как кандидат решил задачу (подход, алгоритм, структуры данных)
2. Какие вопросы задавались и как кандидат отвечал (уровень понимания, техническая глубина)
3. Сильные и слабые стороны кандидата, выявленные в процессе
4. Рекомендации для следующей задачи (на что обратить внимание, какие аспекты проверить)

Резюме должно помочь сгенерировать подходящую вторую задачу, которая учитывает все ответы и уровень кандидата.
  `,

  generateNextTask: (previousTask, candidatePerformance, conversationSummary) => `
Сгенерируй вторую задачу на основе анализа первой задачи и всего разговора.

Первая задача: ${previousTask.description || previousTask.task || 'задача'}
Уровень кандидата: ${candidatePerformance.level || 'Junior'}
Производительность: ${JSON.stringify(candidatePerformance, null, 2)}

Резюме разговора по первой задаче:
${conversationSummary}

ВАЖНО: Вторая задача должна:
1. Учитывать все ответы кандидата на вопросы (не только решение, но и объяснения)
2. Проверять аспекты, которые были слабыми в первой задаче
3. Быть адаптированной к реальному уровню понимания кандидата (не формальному уровню)
4. Логично продолжать проверку навыков, выявленных в разговоре
5. Быть на русском языке (все текстовые поля)

Если кандидат показал хорошее понимание в разговоре, но решение было неоптимальным - задача должна проверить оптимизацию.
Если кандидат плохо объяснял технические детали - задача должна требовать более глубокого понимания.
Если кандидат не учел граничные случаи - задача должна акцентировать их.

Верни ТОЛЬКО валидный JSON без markdown:
{
  "task": "Описание задачи на русском",
  "requirements": ["требование 1", "требование 2"],
  "example": "Пример: вход → выход",
  "hint": "Подсказка на русском",
  "starterCode": "def solution(arr):\\n    pass",
  "visibleTestCases": [...],
  "hiddenTestCases": [...]
}
  `,

  generateFinalQuestion: (taskHistory, currentTask, metrics) => `
Сгенерируй финальный вопрос для обсуждения с кандидатом.

Кандидат выполнил ${taskHistory.length + 1} задач.
Метрики: ${JSON.stringify(metrics, null, 2)}

Спроси о:
- Их опыте решения задач
- Что они узнали
- С какими вызовами столкнулись
- Как они подходили к проблемам

Будь естественным и разговорчивым. Задай один вопрос.
  `,

  generateDialogueResponse: (question, answer, context) => `
Продолжи разговор естественно.

Твой вопрос: ${question}
Ответ кандидата: ${answer}
Контекст: ${JSON.stringify(context, null, 2)}

Ответь вовлекающе и задай следующий вопрос, если уместно.
  `,

  generateReport: (taskHistory, chatHistory, metrics) => `
Сгенерируй комплексный отчет об интервью.

Выполнено задач: ${taskHistory.length}
История задач: ${JSON.stringify(taskHistory.map(t => ({
  level: t.task?.level,
  score: t.score,
  topic: t.task?.topic
})), null, 2)}

История чата: ${JSON.stringify(chatHistory.slice(-10), null, 2)}

Метрики: ${JSON.stringify(metrics, null, 2)}

Предоставь:
1. Strengths: Массив сильных сторон кандидата
2. Weaknesses: Массив областей для улучшения
3. DetailedAnalysis: Детальный текстовый анализ
4. Recommendations: Рекомендации по развитию
5. OverallAssessment: Общая оценка

Верни ТОЛЬКО валидный JSON:
{
  "strengths": ["сильная сторона 1", "сильная сторона 2"],
  "weaknesses": ["слабая сторона 1", "слабая сторона 2"],
  "detailedAnalysis": "Детальный анализ производительности...",
  "recommendations": ["рекомендация 1", "рекомендация 2"],
  "overallAssessment": "Общая оценка кандидата..."
}
  `,

  assessLevel: (code, task, metrics) => `
Определи уровень кандидата на основе его решения первой задачи.

Задача: ${task.description}
Уровень задачи: ${task.level}
Ожидаемая сложность: ${task.expectedComplexity || 'не указана'}

Код кандидата:
\`\`\`python
${code}
\`\`\`

Метрики:
- Время решения: ${metrics.timeSpent}ms
- Попыток: ${metrics.attempts}
- Все тесты прошли: ${metrics.testResults?.allPassed ? 'да' : 'нет'}

Оцени:
1. Correctness (0-100): Правильность решения
2. Optimality (0-100): Оптимальность алгоритма
3. CodeQuality (0-100): Качество кода
4. SuggestedNextLevel: Предлагаемый следующий уровень (Junior, Junior+, Middle, Middle+, Senior)
5. Reasoning: Объяснение оценки
6. Strengths: Сильные стороны
7. Weaknesses: Слабые стороны

Верни ТОЛЬКО валидный JSON:
{
  "correctness": 85,
  "optimality": 70,
  "codeQuality": 80,
  "suggestedNextLevel": "Middle",
  "reasoning": "Кандидат показал хорошее понимание...",
  "strengths": ["Четкая логика", "Хорошие имена"],
  "weaknesses": ["Можно оптимизировать"]
}
  `,

  updateLevel: (currentLevel, taskHistory, latestResult) => `
Обнови оценку уровня кандидата на основе его прогресса.

Текущий уровень: ${currentLevel}
Выполнено задач: ${taskHistory.length}

История задач:
${JSON.stringify(taskHistory.map(t => ({
  level: t.level,
  score: t.score,
  timeSpent: t.timeSpent,
  attempts: t.attempts
})), null, 2)}

Последний результат:
${JSON.stringify(latestResult, null, 2)}

Определи:
1. NewLevel: Новый уровень (Junior, Junior+, Middle, Middle+, Senior)
2. Confidence (0-100): Уверенность в оценке
3. Reasoning: Объяснение изменения уровня
4. Trend: Тренд (improving, stable, declining)

Верни ТОЛЬКО валидный JSON:
{
  "newLevel": "Middle",
  "confidence": 85,
  "reasoning": "Кандидат показал стабильный рост...",
  "trend": "improving"
}
  `,

  selectNextTask: (currentLevel, suggestedLevel, taskHistory, performance, topic) => `
Подбери параметры следующей задачи для кандидата.

Текущий уровень: ${currentLevel}
Предлагаемый уровень: ${suggestedLevel}
Выполнено задач: ${taskHistory.length}

Производительность:
${JSON.stringify(performance, null, 2)}

История задач (последние 3):
${JSON.stringify(taskHistory.slice(-3).map(t => ({
  level: t.level,
  topic: t.topic,
  score: t.score
})), null, 2)}

Предпочитаемая тема: ${topic || 'любая'}

Определи оптимальные параметры следующей задачи:
1. Level: Уровень сложности
2. Topic: Тема (arrays, strings, algorithms, data_structures, и т.д.)
3. Reasoning: Объяснение выбора

Учти:
- Разнообразие тем (не повторять недавние)
- Соответствие уровню кандидата
- Прогрессию сложности

Верни ТОЛЬКО валидный JSON:
{
  "level": "Middle",
  "topic": "algorithms",
  "reasoning": "Кандидат готов к более сложным задачам..."
}
  `
};

export default PROMPTS;

